<?xml version="1.0" encoding="UTF-8"?>

<ruleset name="Zend Developer Tools additional collectors PHP_CodeSniffer Rule Set">
    <description>Zend Developer Tools additional collectors PHP_CodeSniffer Rule Set</description>

    <!-- Import PSR2 entire rule set -->
    <rule ref="PSR2"/>

    <!-- exclude files/directories -->
    <exclude-pattern>*/build/*</exclude-pattern>
    <exclude-pattern>*/dist/*</exclude-pattern>
</ruleset>
